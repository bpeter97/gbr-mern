{
  "name": "gbr-management-system",
  "version": "1.0.0",
  "description": "A management system for Green Box Rentals.",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix client",
    "start": "node server.js",
    "server": "nodemon server.js",
    "test-all": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/*.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=alltests --timeout 5000",
    "test-containers": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/containers.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=containers --timeout 5000",
    "test-products": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/products.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=products --timeout 5000",
    "test-users": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/users.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=users --timeout 5000",
    "test-defaults": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/defaults.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=defaults --timeout 5000",
    "test-events": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/events.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=events --timeout 5000",
    "test-settings": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/settings.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=settings --timeout 5000",
    "test-customers": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/customers.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=customers --timeout 5000",
    "test-todos": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/todos.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=todos --timeout 5000",
    "test-quotes": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/quotes.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=quotes --timeout 5000",
    "test-orders": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/orders.test.js --reporter mochawesome --reporter-options reportDir=server/tests/reports,reportFilename=orders --timeout 5000",
    "test-watch": "nodemon --exec \"yarn run test-all\"",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \" npm run client\""
  },
  "author": "Brian Peter & Taylor Hartley",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "classnames": "^2.2.6",
    "concurrently": "^4.0.1",
    "dotenv": "^5.0.1",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.3.0",
    "jwt-decode": "^2.2.0",
    "lodash": "^4.17.11",
    "mongodb": "^3.1.8",
    "mongoose": "^5.3.2",
    "node-geocoder": "^3.22.0",
    "prop-types": "^15.6.2",
    "react-tooltip": "^3.9.0",
    "validator": "^10.8.0"
  },
  "devDependencies": {
    "expect": "^22.4.3",
    "mocha": "^5.2.0",
    "mochawesome": "^3.0.3",
    "nodemon": "^1.18.4",
    "supertest": "^3.3.0"
  }
}
