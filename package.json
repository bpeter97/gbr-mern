{
  "name": "gbr-management-system",
  "version": "1.0.0",
  "description": "A management system for Green Box Rentals.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \" npm run client\"",
    "test-all": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/*.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=alltests --timeout 5000",
    "test-containers": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/containers.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=containers --timeout 5000",
    "test-products": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/products.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=products --timeout 5000",
    "test-users": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/users.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=users --timeout 5000",
    "test-defaults": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/defaults.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=defaults --timeout 5000",
    "test-events": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/events.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=events --timeout 5000",
    "test-settings": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/settings.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=settings --timeout 5000",
    "test-customers": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/customers.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=customers --timeout 5000",
    "test-todos": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/todos.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=todos --timeout 5000",
    "test-quotes": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/quotes.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=quotes --timeout 5000",
    "test-orders": "SET \"NODE_ENV=test\" && mocha tests/orders.test.js --reporter mochawesome --reporter-options reportDir=tests/reports,reportFilename=orders --timeout 5000",
    "test-watch": "nodemon --exec \"yarn run test-all\""
  },
  "author": "Brian Peter & Taylor Hartley",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "classnames": "^2.2.6",
    "concurrently": "^4.0.1",
    "dotenv": "^5.0.1",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.3.0",
    "jwt-decode": "^2.2.0",
    "lodash": "^4.17.11",
    "moment": "^2.24.0",
    "mongodb": "^3.1.8",
    "mongoose": "^5.3.2",
    "mongoose-deep-populate": "^3.1.1",
    "node-geocoder": "^3.22.0",
    "path": "^0.12.7",
    "prop-types": "^15.6.2",
    "react-tooltip": "^3.9.0",
    "validator": "^10.8.0"
  },
  "devDependencies": {
    "expect": "^22.4.3",
    "mocha": "^5.2.0",
    "mochawesome": "^3.0.3",
    "nodemon": "^1.18.4",
    "supertest": "^3.3.0"
  },
  "engines": {
    "node": "10.12.0"
  }
}
